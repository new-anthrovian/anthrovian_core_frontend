# Copy to .env.local and fill in real values for local dev.
# In production, set these in your host's env-var UI (e.g. Vercel).

# CDN root for video + poster assets. Strip trailing slash.
# Unset/blank => falls back to /videos/ (served from public/videos/).
NEXT_PUBLIC_VIDEO_BASE=
